{
  "name": "ratesassist",
  "version": "0.2.0",
  "private": true,
  "description": "RatesAssist — vertical AI for Australian council rates departments. Production monorepo.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:adapter-demo": "npm run dev --workspace=packages/adapter-demo",
    "ship-check": "bash scripts/ship-check.sh",
    "smoke": "tsx scripts/smoke.ts",
    "perf": "tsx scripts/perf-bench.ts",
    "rotate-secret": "tsx scripts/rotate-secret.ts",
    "dmirs-pull": "tsx scripts/dmirs-lga-pull.ts",
    "grants-poll": "tsx scripts/grants-poll.ts",
    "lag-window": "tsx scripts/lag-window-pull.ts",
    "import-roll": "tsx scripts/import-rating-roll.ts",
    "refresh-rate-tables": "tsx scripts/refresh-rate-tables.ts",
    "prepare": "husky 2>/dev/null || true"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "tsx": "^4.22.4",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": "bash -c 'npm run typecheck --workspaces --if-present'"
  }
}
