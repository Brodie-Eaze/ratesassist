{
  "name": "@ratesassist/contract",
  "version": "0.2.0",
  "private": true,
  "description": "RatesAssist canonical MCP RatesAdapter contract — types, schemas, tool definitions. Every council-platform adapter implements this. Every consumer reads from this.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./schemas": "./src/schemas.ts",
    "./tools": "./src/tools.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  }
}
